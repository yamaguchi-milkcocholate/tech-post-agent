tool_prompt = """
1. download_github_repo(repo_url: str) -> Path:
   - Download a GitHub repository from the given URL and return the local path where it is stored.

    Arguments:
    - repo_url (str): The URL of the GitHub repository to download.

    Returns:
        Path: The local path where the repository is stored.
"""
